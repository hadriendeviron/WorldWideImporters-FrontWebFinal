import { IsPositiveIntegerDirective } from './is-positive-integer.directive';

describe('IsPositiveIntegerDirective', () => {
  it('should create an instance', () => {
    const directive = new IsPositiveIntegerDirective();
    expect(directive).toBeTruthy();
  });
});
